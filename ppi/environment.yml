name: chapi
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.10
  - coot-headless
  - pip
  - pip:
      - fastapi==0.128.2
      - uvicorn==0.40.0
      - pydantic==2.12.5
      - starlette==0.50.0
      - pdbe-arpeggio
      - openbabel-wheel
